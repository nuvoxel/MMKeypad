## Managed dependencies for the audio bring-up image.
dependencies:
  idf: ">=5.1"
  # ES8311 codec control + I2S data path (same component the main fw will use).
  espressif/esp_codec_dev: "^1.3.0"

  # On-screen level meter — same display stack as the main firmware.
  lvgl/lvgl: "~9.3.0"
  espressif/esp_lvgl_port: "^2.4.0"
  espressif/esp_lcd_ili9341: "^2.0.0"
